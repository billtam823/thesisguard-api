Generate a long-term buy thesis for the following stock.

Ticker: {{ticker}}
Company: {{companyName}}
Analyst notes / context (may be empty): {{userNotes}}
Latest fundamentals from data provider (may be empty): {{fundamentalsJson}}

The response shape is enforced by a strict JSON schema, so return ONLY the JSON object it defines.
Keep every string value concise (aim under 160 chars) and arrays to 3-5 items. This endpoint stores
structured monitoring data, not an essay.

Field guidance (semantics the schema can't capture):
- convictionScore: 0-100. GREEN typically 70+, YELLOW 40-69, RED below 40.
- exclusionGates.failedGates: gate ids (G1-G7); empty array when passed is true.
- moat.moatTypes: any of TECHNOLOGY, NETWORK_EFFECT, SCALE, BRAND, SWITCHING_COST, PROPRIETARY_DATA, REGULATORY, DISTRIBUTION, TALENT.
- thesisSummary: 3-5 plain sentences on why this compounds for 5-7 years.
- killCriteria: 3-7 OBSERVABLE, headline-matchable conditions that would BREAK the thesis (consumed by the daily review). Bad: "competition increases". Good: "a rival ships a production product that runs mainstream workloads without our ecosystem at materially lower cost".
- watchItems: specific metrics/events to monitor quarterly.
- upgradeMilestones: only for YELLOW (what would upgrade it to GREEN); empty array otherwise.
- growthForecast: follow Stage 6. Return = revenueGrowthFactor(CAGR) x (terminalP/S / currentP/S), then apply the size cap. If fundamentals provide currentPriceToSales you MUST use it and set confidence HIGH; otherwise estimate it and set MEDIUM/LOW. Echo the P/S you used in currentPriceToSales.
